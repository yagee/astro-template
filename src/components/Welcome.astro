---
interface Props {
  title: string;
  body: string;
}

const { title, body } = Astro.props;

import pic from './bald.png';
---

<div class="card">
  <h1>
    {title}
  </h1>
  <p>
    {body}
  </p>
  <img
    src={pic.src}
    alt=""
  />
</div>

<style>
  h1 {
    font-size: 20px;

    @media screen and (width >= 375px) {
      font-size: 22px;
    }

    @media screen and (width >= 414px) {
      font-size: 24px;
    }
  }

  .card {
    color: rgb(var(--accent));
    z-index: 5000;
  }

  .card {
    color: green;
  }
</style>
